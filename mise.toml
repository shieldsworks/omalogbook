# `mise install` gets the toolchain; `mise tasks` lists the jobs and each
# runs as `mise <job>`.

[tools]
rust = { version = "1.98", profile = "minimal", components = "rustfmt,clippy" }

[tasks.build]
description = "Debug build"
run = "cargo build --locked"

[tasks.test]
description = "Unit tests"
run = "cargo test --locked"

[tasks.lint]
description = "rustfmt check and clippy with warnings denied"
run = [
  "cargo fmt --check",
  "cargo clippy --locked --all-targets -- -D warnings",
]

[tasks.format]
description = "rustfmt, writing"
run = "cargo fmt"

[tasks.replay]
description = "Write a log from omakeel's sample sail into a scratch vault"
run = [
  "cargo run --locked -- run --vault /tmp/omalogbook-replay --no-git",
]
